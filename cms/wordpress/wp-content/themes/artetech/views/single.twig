{% extends "index.twig" %}

{% block main %}
    <h1> SINGLE Page </h1>
    
    {{ periode.klantperiode }}
     {{ periode.startdatum }}

      {% for techlog in techlogs %}
    
            <h2> TECHLOGS</h2>
            <p>{{ techlog.datum }} </p>
            <p>{{ techlog.title }} </p>
            <p>{{ techlog.klant }} </p>

        {% endfor %}

    <form action="{{ action }}" method="POST">
        <input type="hidden" name="action" value="your_action_name">
        <input type="hidden" name="id" value="{{ periode.id }}">
        <input type="hidden" name="ids" value="{{ techlog_ids }}">
        <input type="text" name="comment" value="">

        <input type="submit" name="submit" value="ondertekenen">
    </form>

{% endblock %}